node_modules
# Keep environment variables out of version control
.env
# Local Postgres data (use Docker named volume in production)
postgres_data

/generated/prisma
postgres_data/

# Downloaded MP3s (run npm run download:songs to fetch)
songs/
